# Что загружать в репозиторий

## Обязательно

- исходный код (`src/`)
- `CMakeLists.txt`
- `README.md`
- папка `docs/`

## Не загружать

- `.vs/`
- `out/`, `build/`
- временные файлы и бинарники (`.exe`, `.pdb`, `.obj`, `.log`)

Это уже учтено в `.gitignore`.
